import { StyleSheet } from 'react-native'

const ScreensSS = StyleSheet.create({
    conteiner: {
        flex: 1,
        flexDirection: 'column',
        justifyContent: 'space-evenly',
        alignItems: 'center',
        backgroundColor: '#372775',
    }
})

export default ScreensSS